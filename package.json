{
  "name": "global-changelog",
  "version": "0.1.1",
  "description": "Generate global changelog from several packages for some time interval",
  "bin": {
    "global-changelog":"dist/bin/global-changelog.js"
  },
  "files": [
    "dist"
  ],
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "set NODE_ENV=production&&babel src --out-dir dist"
  },
  "author": "KukuruzaAndrey",
  "license": "ISC",
  "dependencies": {
    "showdown": "^1.8.6",
    "simple-git": "^1.102.0"
  },
  "devDependencies": {
    "babel-preset-env": "^1.7.0"
  }
}
